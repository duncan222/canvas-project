import "./index.scss";
import "../Layout/index.scss";

const Profile = () => {
  return (
    <div className="container profile-page">
      <div className="text-zone">
        <h1>
          this is the profile page, where <br /> the user will customize their
          page and also sign out.
        </h1>
      </div>
    </div>
  );
};

export default Profile;
